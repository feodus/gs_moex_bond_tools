# Руководство по работе с ESLint и Prettier

Полная инструкция по использованию ESLint и Prettier в проекте `gs_moex_bond_tools`.
Ссылка на таблицу - https://docs.google.com/spreadsheets/d/14QN-103oPdywat3fKgZyde1zmVU4lvWSG1LIk8OlKx8/edit?usp=sharing

---

## 📋 Оглавление

1. [Структура Monorepo](#структура-monorepo)
2. [Как работают плагины](#как-работают-плагины)
3. [Настройки VS Code](#настройки-vs-code)
4. [Конфигурационные файлы](#конфигурационные-файлы)
5. [Ручной запуск из командной строки](#ручной-запуск-из-командной-строки)
6. [Примеры использования](#примеры-использования)
7. [Устранение проблем](#устранение-проблем)

---

## Структура Monorepo

Этот репозиторий использует **monorepo** подход — все настройки ESLint и Prettier вынесены на корневой уровень `/Users/pl/Documents/google_script/`.

### Преимущества:

✅ **Единая конфигурация** для всех проектов Google Apps Script  
✅ **Одна установка** зависимостей — `node_modules` только в корне  
✅ **Легко добавлять новые проекты** — просто создаете папку с `.js` файлами  
✅ **Централизованное обслуживание** — обновление настроек в одном месте

### Структура репозитория:

```
google_script/                          ← КОРЕНЬ РЕПОЗИТОРИЯ
├── .eslintrc.json                      ← Правила ESLint
├── .prettierrc                         ← Правила Prettier
├── .vscode/settings.json               ← Настройки VS Code
├── package.json                        ← Зависимости
├── node_modules/                       ← Установленные пакеты
├── docs/                               ← Документация
│   └── ESLINT_PRETTIER_GUIDE.md
├── gs_moex_bond_tools/            ← Проект 1
│   ├── script.js
│   └── appscript.json
└── [другие проекты]/                   ← Будущие проекты
    ├── script.js
    └── appscript.json
```

> [!IMPORTANT]
> Все команды `npx eslint` и `npx prettier` запускаются **из корня репозитория** (`/Users/pl/Documents/google_script/`), а не из папок отдельных проектов.

---

## Как работают плагины

### ESLint — Постоянное отслеживание 🔍

**Режим работы:** В реальном времени

ESLint активен постоянно и анализирует код **на лету**:

- ✅ Работает автоматически при открытии любого `.js` файла
- ✅ Анализирует код по мере набора
- ✅ Показывает ошибки сразу:
  - 🔴 Красные волнистые линии — ошибки (errors)
  - 🟡 Желтые волнистые линии — предупреждения (warnings)
- ✅ Отображает список проблем в панели **Problems** (внизу VS Code)
- ✅ Автоисправление запускается при сохранении файла

#### Что проверяет ESLint:

- Синтаксические ошибки JavaScript
- Стиль кода (использование `var` vs `const`/`let`)
- Неиспользуемые переменные
- Правила форматирования от Prettier (через плагин)

---

### Prettier — Форматирование по триггеру 💾

**Режим работы:** При сохранении файла

Prettier срабатывает **только при сохранении**:

- ✅ Активируется триггером `Cmd+S` (macOS) / `Ctrl+S` (Windows/Linux)
- ✅ Автоматически форматирует весь файл
- ⚠️ **НЕ работает** в реальном времени во время набора
- ✅ Выравнивает код согласно настройкам в `.prettierrc`

#### Что форматирует Prettier:

- Отступы (табуляция/пробелы)
- Длина строк (переносы)
- Кавычки (`'` vs `"`)
- Пробелы вокруг операторов
- Trailing commas (запятые после последнего элемента)
- Точки с запятой

---

## Настройки VS Code

Файл: [.vscode/settings.json](file:///Users/pl/Documents/google_script/.vscode/settings.json)

```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": "explicit"
  },
  "[javascript]": {
    "editor.defaultFormatter": "esbenp.prettier-vscode"
  }
}
```

### Что делает каждая настройка:

| Настройка                 | Описание                                    |
| ------------------------- | ------------------------------------------- |
| `editor.formatOnSave`     | Включает автоформатирование при сохранении  |
| `editor.defaultFormatter` | Назначает Prettier форматтером по умолчанию |
| `source.fixAll.eslint`    | Автоисправление ESLint при сохранении       |
| `[javascript]`            | Специфичные настройки только для JS файлов  |

---

## Конфигурационные файлы

### `.eslintrc.json` — Правила ESLint

```json
{
  "extends": ["eslint:recommended", "plugin:prettier/recommended"],
  "env": {
    "es6": true,
    "node": true,
    "googleappsscript/googleappsscript": true
  },
  "plugins": ["googleappsscript"],
  "parserOptions": {
    "ecmaVersion": 2020
  },
  "rules": {
    "prettier/prettier": "error"
  }
}
```

**Ключевые моменты:**

- `eslint:recommended` — базовые правила ESLint
- `plugin:prettier/recommended` — интеграция с Prettier
- `googleappsscript/googleappsscript` — понимание глобальных объектов GAS (SpreadsheetApp, Logger и т.д.)
- `ecmaVersion: 2020` — поддержка ES6+ синтаксиса

---

### `.prettierrc` — Правила форматирования

```json
{
  "singleQuote": true,
  "trailingComma": "es5",
  "printWidth": 100,
  "tabWidth": 2,
  "semi": true
}
```

**Что означают настройки:**

| Правило         | Значение | Описание                                                |
| --------------- | -------- | ------------------------------------------------------- |
| `singleQuote`   | `true`   | Использовать одинарные кавычки `'` вместо `"`           |
| `trailingComma` | `"es5"`  | Запятая после последнего элемента (где разрешено в ES5) |
| `printWidth`    | `100`    | Максимальная длина строки 100 символов                  |
| `tabWidth`      | `2`      | Отступ = 2 пробела                                      |
| `semi`          | `true`   | Всегда ставить точку с запятой в конце                  |

---

## Ручной запуск из командной строки

### Предварительные требования

Убедитесь, что вы находитесь в папке проекта:

```bash
cd /Users/pl/Documents/google_script/gs_moex_bond_tools
```

---

### ESLint — Проверка и автоисправление

#### Проверить один файл

```bash
npx eslint script.js
```

#### Проверить все JS файлы в проекте

```bash
npx eslint .
```

#### Автоматически исправить проблемы

```bash
npx eslint script.js --fix
```

> **Примечание:** `--fix` исправляет только автоматически исправимые проблемы (форматирование, простые правила). Логические ошибки нужно исправлять вручную.

#### Проверить только определенные типы ошибок

```bash
# Только ошибки (errors), игнорировать warnings
npx eslint script.js --quiet
```

---

### Prettier — Форматирование

#### Проверить, требуется ли форматирование (без изменений)

```bash
npx prettier script.js --check
```

Вывод:

- ✅ `Checking formatting...` — файл уже отформатирован
- ❌ `script.js` — файл требует форматирования

#### Отформатировать файл (внести изменения)

```bash
npx prettier script.js --write
```

#### Отформатировать все JS файлы в проекте

```bash
npx prettier "**/*.js" --write
```

#### Показать, как будет выглядеть файл после форматирования (без изменений)

```bash
npx prettier script.js
```

Вывод будет в терминале, файл не изменится.

---

### Комбинированная команда: ESLint + Prettier

Сначала форматирование, потом проверка:

```bash
npx prettier script.js --write && npx eslint script.js --fix
```

---

## Примеры использования

### Пример 1: Проверка перед коммитом в Git

```bash
# Отформатировать все файлы
npx prettier "**/*.js" --write

# Проверить на ошибки
npx eslint .

# Если есть автоисправимые ошибки
npx eslint . --fix
```

---

### Пример 2: Работа с конкретным файлом

```bash
# 1. Отформатировать
npx prettier script.js --write

# 2. Проверить и автоисправить
npx eslint script.js --fix

# 3. Показать оставшиеся проблемы
npx eslint script.js
```

---

### Пример 3: Проверка без изменений (CI/CD)

```bash
# Проверить форматирование (вернет код ошибки, если не отформатировано)
npx prettier "**/*.js" --check

# Проверить на ошибки линтинга
npx eslint .
```

---

## Устранение проблем

### ❌ ESLint не работает в VS Code

**Проблема:** Не видно волнистых линий, панель Problems пуста.

**Решение:**

1. Убедитесь, что установлено расширение ESLint для VS Code:
   - Нажмите `Cmd+Shift+X`
   - Найдите "ESLint" (автор: Microsoft)
   - Установите и перезагрузите VS Code

2. Проверьте вывод ESLint в панели Output:
   - `Cmd+Shift+U` → выберите "ESLint" из dropdown

3. Перезапустите ESLint сервер:
   - `Cmd+Shift+P` → "ESLint: Restart ESLint Server"

---

### ❌ Prettier не форматирует при сохранении

**Проблема:** Файл не форматируется автоматически.

**Решение:**

1. Проверьте, что установлено расширение Prettier:
   - `Cmd+Shift+X` → "Prettier - Code formatter" (esbenp.prettier-vscode)

2. Убедитесь, что Prettier выбран форматтером по умолчанию:
   - Откройте любой `.js` файл
   - `Cmd+Shift+P` → "Format Document With..."
   - Выберите "Prettier - Code formatter"
   - Установите как default

3. Проверьте настройки:
   - `Cmd+,` → найдите "format on save"
   - Убедитесь, что галочка стоит

---

### ❌ Конфликт между ESLint и Prettier

**Проблема:** ESLint жалуется на форматирование, которое делает Prettier.

**Решение:**

Это уже решено в конфигурации через `eslint-config-prettier`. Если проблема возникла:

```bash
# Переустановите зависимости
npm install --save-dev eslint-config-prettier eslint-plugin-prettier
```

Убедитесь, что в `.eslintrc.json` есть:

```json
{
  "extends": ["eslint:recommended", "plugin:prettier/recommended"]
}
```

---

### ❌ ESLint показывает: "Cannot find module 'eslint-plugin-googleappsscript'"

**Проблема:** Плагин не установлен.

**Решение:**

```bash
npm install --save-dev eslint-plugin-googleappsscript
```

---

### ⚠️ ESLint версия 9 vs 8

**Проблема:** ESLint 9 требует новый формат конфигурации (`eslint.config.js`).

**Текущее решение в проекте:** Используется ESLint 8.x для совместимости с `.eslintrc.json`.

Если хотите обновиться до ESLint 9, нужно:

1. Конвертировать `.eslintrc.json` → `eslint.config.js`
2. Обновить синтаксис конфигурации

**Рекомендация:** Пока оставайтесь на ESLint 8 для стабильности.

---

## Дополнительные команды

### Показать версии установленных пакетов

```bash
npm list eslint prettier
```

### Обновить ESLint и Prettier

```bash
npm update eslint prettier eslint-config-prettier eslint-plugin-prettier
```

### Удалить node_modules и переустановить

```bash
rm -rf node_modules package-lock.json
npm install
```

---

## Полезные ссылки

- [Документация ESLint](https://eslint.org/docs/latest/)
- [Документация Prettier](https://prettier.io/docs/en/)
- [Google Apps Script ESLint Plugin](https://www.npmjs.com/package/eslint-plugin-googleappsscript)
- [Интеграция ESLint + Prettier](https://github.com/prettier/eslint-plugin-prettier)

---

## Быстрая шпаргалка

| Действие                 | Команда                          |
| ------------------------ | -------------------------------- |
| Проверить код            | `npx eslint script.js`           |
| Автоисправить            | `npx eslint script.js --fix`     |
| Форматировать            | `npx prettier script.js --write` |
| Проверить форматирование | `npx prettier script.js --check` |
| Все файлы (ESLint)       | `npx eslint .`                   |
| Все файлы (Prettier)     | `npx prettier "**/*.js" --write` |

---

**Последнее обновление:** 2025-12-01
